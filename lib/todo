photo as background (if they want main menu, add an animation where it zooms out and it blurs and the items pop up with an animation)
animation when clicking an item
different font
catergories at the top
